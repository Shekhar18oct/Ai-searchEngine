services:
  - type: web
    name: scholarsphere
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.6
      - key: PORT
        value: 5000
